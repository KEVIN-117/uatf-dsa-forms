version: "3.8"

services:
  uatf-forms:
    image: ${DOCKERHUB_USERNAME:-kevin117}/uatf-forms:${IMAGE_TAG:-latest}
    container_name: uatf-forms
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    restart: unless-stopped
