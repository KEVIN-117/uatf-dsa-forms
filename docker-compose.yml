version: "3.8"

services:
  uatf-forms:
    image: ${DOCKERHUB_USERNAME}/uatf-forms:${IMAGE_TAG}
    container_name: uatf-forms
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    restart: unless-stopped
