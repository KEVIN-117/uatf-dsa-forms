version: '3.8'

services:
  uatf-forms:
    # Cambiar 'latest' por 'dev' para el ambiente de desarrollo
    image: ${DOCKERHUB_USERNAME}/uatf-forms:${IMAGE_TAG:-latest}
    container_name: uatf-forms
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    restart: unless-stopped
